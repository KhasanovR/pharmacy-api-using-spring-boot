spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharmacy
    username: rakhmatjon
    password: 2729
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  error:
    include-message: always
  port: 8080
logging:
  file:
    name: ./logs/pharmacy-api.log
